function toggleMenu() {
    const toggleMenu = document.querySelector("toggleMenu");
    const nagivation = document.querySelector(".navigation");
    toggleMenu.classList.toggle("active");
    nagivation.classList.toggle("active");
}